# Decision Log

## Status legend

- Accepted: ユーザーが明示した確定事項
- Proposed: 勝つための設計案。ユーザー承認前
- Open: 未決事項

| ID | Status | 決定・論点 | 理由・備考 |
|---|---|---|---|
| D001 | Accepted | 新入生と部活・サークルをマッチングする | ゼミ課題 |
| D002 | Accepted | スマートフォン向けWebアプリ | ユーザー指定 |
| D003 | Accepted | 団体側から新入生へオファー・案内を届ける | 従来の自発応募の心理的障壁を下げる |
| D004 | Accepted | 新入生は興味分野を事前登録する | アウトドア等の関心に基づく配信 |
| D005 | Accepted | GitHubを正本として使う | CodexとClaudeの知識を共有する |
| D006 | Accepted | Claude Maxを利用できる | Claude Code等を使用可能 |
| D007 | Proposed | 団体には学生個人一覧を見せず、集計条件へ配信する | 差別・プライバシー・スパム対策 |
| D008 | Proposed | 学生が受信カテゴリと週間上限を設定する | 招待の気軽さと利用者制御の両立 |
| D009 | Proposed | 返答は3段階にする | 断りづらさを下げる |
| D010 | Proposed | 初期マッチングは説明可能なルールベース | デモの安定性と説明性 |
| D011 | Proposed | ワーキングタイトルは「CUE」 | 「参加のきっかけが届く」を表現 |
| D012 | Accepted | Vite + React + TypeScript + localStorage + Vitest | 数日で安定した静的デモを作る |
| D013 | Accepted | 2026年8月22日（土）までにデモ版完成 | メンバー間で持ち寄る |
| D014 | Accepted | ゼミメンバーが各自の成果物を持ち寄って比較する | 動作と第一印象が重要。詳細な採点表は不明 |
| D015 | Open | 神戸大学固有か汎用デモか | データと表現に影響 |
| D016 | Proposed | GitHub Pagesで共有URLを用意する | 持ち寄り時の再現性を高める |
| D017 | Accepted | 認証・本番DB・実通知はデモ対象外 | コア体験の完成度を優先する |
| D018 | Proposed | 架空大学・架空団体のデモデータを使う | 実在団体との混同を防ぐ |
| D019 | Accepted | 費用は学生予算・オファー参加費とも「1回あたり（円/回）」で統一する | 根拠のない月額換算は行わない。Task 003のUIも「1回あたりの予算」と表示する。月額比較へ変更する場合は別途仕様決定する |
| D020 | Accepted | 受信停止・週上限は「新規配信の判定」に適用する。これらの設定変更だけでは、表示中の受信済み案内を消さない | Task 004では配信イベントが未実装のため、受信箱の表示集合は現在の興味パスポートによる再評価で導出する暫定仕様（興味・受信カテゴリの編集は表示へ反映される）。Task 005で配信イベントを保存し、受信済み集合を不変の履歴として固定する。デモではdemoOffersのeligible 3件を受信済み相当として表示し、週上限を含む配信判定はTask 005の送信処理で実装する |
| D021 | Accepted | 週上限の「週」は判定時点から遡る7日間のローリングウィンドウとし、`now − 7日 < deliveredAt ≤ now`（下限exclusive・上限inclusive・UTCミリ秒比較・未来時刻は数えない）で判定する。学生の週間受信上限と、団体の週3キャンペーン上限（「1週間に作成できる有効キャンペーン」を直近7日間の送信数と解釈）の両方に配信時点で適用する | 固定の週始まりを持たず決定的。現在時刻は純関数の引数（nowIso）として注入する。過去日付のシード配信は現在週の枠を消費しない |
| D022 | Accepted | 団体ファネルは、配信（recipientの一意人数）・閲覧（read markまたは何らかのresponseを持つ受信者の一意人数）・関心（interestedまたはthinkingの返答を持つ受信者の一意人数）・参加意向（interestedの返答を持つ受信者の一意人数）の匿名件数とし、すべて受信者集合との積集合をstudentId単位で重複排除して数える。独立保存せず配信イベント・既読・返答から導出する。見送り件数は表示しない。第4指標の表示名は「参加意向」とし、「行ってみたい」の回答数であり参加の確約ではない旨を注記する | 配信⊇閲覧⊇関心⊇参加意向が保存障害時（既読保存失敗など）にも単調に保たれる。正本の一本化。「行ってみたい」の非確約性（matching_and_safety §6）と整合 |
| D023 | Accepted | 配信イベント（OfferDelivery）を送信済みキャンペーンと受信箱の唯一の正本とし、オファー内容全体（ClubOffer snapshot）と受信者別のscore・理由・注意点snapshotを配信時点の値で内蔵・固定する。再送禁止は、同一オファーIDの配信冪等化に加え、団体・正規化イベント名・日時・正規化場所が一致する同一イベントを別IDでも送信拒否することで実装し、確認画面と送信確定処理の両方で判定する | D020が予告した履歴固定の完成。パスポート編集は今後の配信判定にのみ影響し、受信済み表示（内容・理由とも）を変えない。1レコード=1書込で原子的に保存する |
| D024 | Accepted | マッチ人数の実計算のため架空の匿名学生プール（19人・PIIなし・表示名「匿名新入生NN」）を導入する。母集団は現在のメイン学生＋プールの20人。団体側画面には人数と匿名集計のみを表示し、個人の属性・一覧は表示しない。初期3件の配信履歴シードは、現在の保存済みパスポートではなくcanonicalなdemoStudentとプールから決定的に生成する | 「12人とマッチ」を演出でなく計算で成立させる（D007の実装形）。シードがユーザーの編集状態に依存せず、Task 004期の既読・返答レコードとofferIdで接続する |
| D025 | Accepted | デモリセットはCUEの4キー（preference・offer-responses・offer-reads・offer-deliveries）だけをsnapshot/rollback付きで削除して直ちにfull reloadし、完了通知はsessionStorageの一時フラグ、確認UIはnative dialogとし、新しいlocalStorage schemaは追加しない | PR #6実装の追認（レビューNon-blocker回収）。localStorage.clear()は使わず無関係キーを保護する。Task 007のscope exceptionとして承認のうえ記録 |
| D026 | Accepted | Phase 2への移行を承認する。D017（認証・本番DB・実通知はデモ対象外）は2026-08-22デモ（Phase 1）限定の決定とし、Phase 2では①大学メールによる登録・ログイン ②新入生と団体担当者の権限分離 ③一人が複数役割を持てる設計 ④団体を共有アカウントにせず複数担当者が所属する組織として管理 ⑤localStorageからサーバーデータへの移行 ⑥オファー到着時の登録メールへの通知 ⑦団体側に学生のメール・氏名・学籍番号を非表示、を実装する | ユーザー承認（2026-08-24）。Task 008（認証・権限基盤）/ 009（サーバーデータ移行）/ 010（メール通知）/ 011（運用・セキュリティQA）の4PRへ分割。mainと公開デモは凍結し、PRのbaseは`develop` |
| D027 | Accepted | Phase 2の技術はSupabase Auth（メールOTP）+ PostgreSQL + RLSを採用する。認証・権限はSQL（SECURITY DEFINER RPC + RLS）で完結させ、secret/service-role keyをフロントエンド・CIへ持ち込まない。Edge Functions + ResendはTask 010の通知まで導入しない | 原子性・pgTAP検証・鍵管理の観点でSQL RPCを優先。既存localStorageデモは隔離保持し、データ移行はTask 009 |
| D028 | Accepted | メール許可規則: 正規化（trim+小文字化）後にドメインが`stu.kobe-u.ac.jp`へ完全一致するものだけを許可する。サブドメイン・類似ドメインは不可。ローカル部に`+`を含むアドレスは拒否する（plus addressingによる複数アカウント化防止）。学籍番号の文字種など、これ以上に狭い形式は推測しない | D015は「少なくともPhase 2の認証は神戸大学学生メール限定」として解決。判定表はTypeScript関数とSQL関数で同一とし、双方でテストする |
| D029 | Accepted | D007を恒久要件化する。団体向けのtable・view・RPC・生成TypeScript型に、学生のメール・氏名・学籍番号・`auth.users.id`・配信対象学生ID一覧を含めない。大学メールは`auth.users`だけに保持し、ローカル部（学籍番号相当の機密）を`public`スキーマ・ログ・テスト出力へ保存しない | Task 009の集計APIも匿名件数のみを返す前提。担当者表示はPIIを含まないランダムラベルに限定する |
| D030 | Accepted | Before User Created Hookは採用しない（現行プランではTeam以上限定のため）。認証境界は①クライアントがドメイン外メールのOTP送信を拒否 ②`is_university_user()`が現在の`auth.users.email`（正規化判定+`email_confirmed_at IS NOT NULL`）をサーバー側で確認 ③すべてのRLSとSECURITY DEFINER RPCが同関数を必須条件とする、の3層とする | 悪意ある利用者がAuth API直接呼出でドメイン外のauth identityを作成し得ることは残余リスクとして受容し、そのidentityはCUEの全データ・RPCへアクセス不能にする。CAPTCHA・レート制限・identity掃除運用はTask 011 |
| D031 | Accepted | Task 008はPhase A（ローカル実装: migration・RLS/RPC・pgTAP・フロントエンド・Mailpit OTP・CI・lint/test/build・PII/secret検査）とPhase B（hosted staging: migration手動適用・本人所有の大学メール1件でのOTP実機確認ほか）の二段階で完了判定する。Phase B完了までは「実装完了・hosted検証待ち」とする | 実装開始前に`main`へbranch ruleset（PR必須・force push禁止・削除禁止）を人間が設定し、`develop`作成直後にも同等の保護を設定する。手順は`docs/runbook_supabase_hosted.md` |
| D032 | Accepted | Task 009のサーバーデータ設計: 興味パスポートは`public.student_passports`（自分の行のみSELECT可・書込は`save_student_passport` RPCのみ）、配信・受信者snapshot・既読・返答はprivateスキーマ4テーブル+SECURITY DEFINER RPC限定とする。マッチングは`private.match_passport()`をサーバー正本とし、`domain/matching.ts`と同一判定表をTS/SQL両側の同一ケース表（C01〜C16）で検証する（D028のメール判定と同じ双実装パターン）。D021の週枠（学生週上限+団体週3）・D022のファネル導出・D023のsnapshot固定と同一イベント再送禁止（fingerprint一意制約）をすべてサーバー側で強制し、オファー送信・プレビューは団体のowner/admin+verified状態を必須とする。団体向けRPCは匿名件数のみを返す（D029） | 正本は`docs/server_data_model.md`。008の`auth_and_authorization.md` §11の契約（FK先・is_university_user必須・インラインEXISTS・private+RPCパターン・明示revoke/grant・pgTAP/生成型/CI踏襲）に全面的に従う。Task 009 PRのレビューで確認 |
| D033 | Accepted | 団体の公式窓口を`organizations.contact_label / contact_handle`（既定は空文字）として追加し、owner/adminが`update_organization_contact` RPCで編集する。学生へは「行ってみたい」を選んだ後にだけ、配信時snapshotの値を開示する。個人の連絡先は登録しない | matching_and_safety.md §6「行ってみたい→参加方法と団体の公式連絡先を表示する」とPhase 1の`Club.contact`のサーバー移植。未登録（空）の場合、学生の同意後パネルには窓口未登録の案内を表示する |
| D034 | Accepted | 旧デモ（`cue-demo:*`）からの持ち込み移行は、本人が入力した興味パスポート1キーのみを対象に、認証済み学生コンテキストの初回に一度だけ行う。schema検証+wizard必須条件を通る値だけを`save_student_passport`（サーバー側でも再検証）で保存し、成功したときだけ4キーすべてを削除する。保存失敗時はキーを残して次回再試行し、破損・改ざん値と配信・既読・返答の3キー（架空デモ団体に紐づく演出データ）は取り込まずに削除する。同処理は冪等で、複数回実行しても重複・消失は起きない | localStorage値を信頼済みデータとして無検証で取り込まない。公開デモ（main・GitHub Pages）とデモ本体`src/demo/`には触れない。auth_and_authorization.md §10が保留していた「移行・削除の判断」の確定 |
| D035 | Accepted | AIエージェントの運用を逐次確認型から自律実行型へ変更する。`docs/agent_harness.md`を実行規約の正本とし、実行モード（Fast/Standard/Deep）・Plan→Implement→Verify→Review→Repairループ・Best-of-N（設計のみ複数案、実装は1案）・Definition of Done・人間へのエスカレーション範囲・ハーネス改善ルールを定義する。独立レビューは`.claude/agents/`のsubagent（architect / test-engineer / reviewer / security-reviewer）で行い、認証・DB・RLS・RPC・migration・Edge Function・通知・PIIに触れる変更ではsecurity-reviewerを必須とする。`.claude/hooks/`のPreToolUse hookで保護ブランチへの直接push・force push・`git reset --hard`・`git clean -f`・`git branch -D`を拒否し、Stop hookで`app/`変更時のlint/unit test/buildをローカルゲートとする。`.claude/settings.json`の`permissions.deny`で`.env`系ファイルと`.git/config`のReadツールでの読み取りを拒否する（`app/.env.example`は読める） | 人間の承認待ちが実装の律速になっていたため。停止するのはOAuth/OTP・課金・production不可逆変更・正本の重大な矛盾など人間判断が必要な場合だけとし、コマンドやCIの失敗では停止しない。hookはローカルの早期発見手段であり、実効的な保護はブランチ保護とCIが担う（Stop hookは`stop_hook_active`により1セッション1回までブロック）。重いDBテスト・E2EはタスクのDoDとCIで担保し、Stop hookでは実行しない。自動マージはしない |
| D036 | Accepted | オファー配信の最小対象人数を5人とする。0人は`no_recipients`、1〜4人は`insufficient_audience`として**DB/RPC側で**拒否する。判定はクライアントに置かず、送信時点でサーバーが対象集合を再計算するため、preview後に人数が変わっても基準を破れない。対象規模のpreviewは正確な人数を返さず、`0` / `1-4` / `5-9` / `10-24` / `25-49` / `50+` の6区分だけを返す。生の人数はAPI応答・エラーメッセージ・ログ・クライアント状態に一切現れない（`send_offer`の戻りも区分のみ） | 少人数への配信は受信者の特定につながり、正確な人数は差分攻撃の観測値になる（matching_and_safety.md §7「個人特定」）。**対象規模**（preview・send）の区分変換点は`private.audience_band()`のみ。配信後のファネルは別の量で、D037の10-5ルール（5人単位の丸め）に従う。検証は`17_min_audience_test.sql`・`18_preview_band_test.sql`・`23_input_boundary_test.sql`。**残余リスク（運営者の判断が必要）**: ①集団全体が対象から外れる条件では区分が動く（「その条件では誰も該当しない」という集計情報であり個人の特定ではない） ②区分が`0`と`1-4`を区別するため、**previewの予算（20条件/24時間）の範囲内では「小グループの在／不在」を1人単位で観測できる**。独立レビューで実証済み。塞ぐには`0`と`1-4`を1区分へ統合する必要があり、本決定（6区分）の改定を要するため、閉鎖βの規模と運用（団体のverifyは運営の手動）を踏まえた判断を運営者へ委ねる |
| D037 | Accepted | 団体向けファネルの開示規則を**英国ONSの10-5ルールを参考に**定める。①配信人数10人未満のofferはファネルを一切開示しない ②10人以上でも各セルが10未満なら数値を返さず抑制（NULL）する ③開示する数値は5人単位へ四捨五入する ④パーセント・比率は返さない ⑤リアルタイム更新せず1日1回の安定したsnapshot（`snapshot_date`はAsia/Tokyoの暦日）を返す ⑥UIは抑制を「0人」と表示せず「—」と非表示の理由を併記する。あわせて学生の週間受信枠を`private.student_delivery_quota`（学生1人1行）で管理し、`send_offer`が対象学生の行を**user_id昇順で1行ずつ`FOR UPDATE`**して確保することで、団体をまたぐ並行配信でも上限が破れないようにする | **法令準拠を主張するものではない**（参考にした公開手法であり、運営者による最終確認が必要）。パーセントを返すと抑制セルを逆算できるため返さない。週の定義はD021のローリング7日を維持し、`timestamptz`同士＝UTCの絶対時刻で比較する。上限判定を自トランザクションの`now()`で閉じると、後からcommitされた並行配信を取りこぼすため、確保後の再判定では上限を閉じない。検証は`21_funnel_suppression_test.sql`と`22_concurrent_quota_test.sql`（単一セッションで決定的に確認できる部分）、および`app/scripts/concurrency_test.sh`（psqlを複数プロセス起動して**実際に並行するトランザクション**を作り、ロック待ち・不変条件・部分配信なし・4団体同時起動を確認。CIの`db-tests`が`npm run db:test:concurrency`として実行）。dblinkは**Supabaseのローカルスタックで`postgres`がsuperuserではなく`dblink_connect_u`を実行できない**ため採用しない。**残余リスク**: 丸めが決定的（12→10 / 13→15）のため、毎日ファネルを見る団体は閾値をまたいだ1人の返答日を特定できる。ONSがrandom rounding / barnardisationを推奨するのはこの差分対策のためで、本実装は未対応（運営者の判断でTask 017以降の課題とする） |
| D038 | Accepted | 対象規模previewの結果は、同一条件について**24時間固定**する。条件はマッチ判定へ影響する7項目（対象カテゴリ・対象目的・本気度・開催曜日・頻度・参加費・初心者対応）だけを正規化してSHA-256でfingerprint化し、`private.offer_preview_cache`へ**fingerprintと区分のみ**を保存する。団体単位で**rolling 24時間あたり20条件**までに制限し、同一条件の再問い合わせは回数を消費しない。イベント名・日時・場所・説明文はfingerprintに含めない | 24時間固定により、学生1人が設定を変えたことを団体が観測できない（差分攻撃の観測窓を閉じる）。回数制限により探索の手数が有界になる。rawの学生希望条件はもちろん、団体の指定条件も平文で監査へ残さない。ただしfingerprintは**saltもHMAC鍵も無い素のSHA-256**で、探索空間が約5×10^10と小さいため**秘匿性は主張しない**（独立レビューで逆算を実証）。表のgrantはゼロで、DB自体が漏えいしない限り到達できないことに依拠する。イベント名等を含めると、同じ対象条件のまま文言だけ変えて回数制限を回避できるため含めない。配列は順序非依存に正規化する（マッチ判定が集合演算のみに依存するため）。**`send_offer`は、同一fingerprintの24時間以内のpreviewが存在しない送信を`preview_required`で拒否する**（product_spec.md §7 団体の「4. プレビュー → 5. 送信」導線と一致）。これが無いと、送信の失敗は全体をrollbackして何も消費・記録しないため、`no_recipients`（0人）と`insufficient_audience`（1〜4人）の区別が「回数制限も監査記録も無い1人単位の判定器」になり、本決定の制限を完全に迂回できる（独立レビューで、標的1人の受信カテゴリ・本気度・曜日・予算上限を30回のprobeで復元できることを実証）。古いキャッシュ行の削除は未実装でTask 017へ引き継ぐ。検証は`19_preview_quota_test.sql`・`20_differential_attack_test.sql` |
| D039 | Accepted | オファー入力の検証を2段にする。①`private.assert_offer_input_bounds()`が、生の引数のまま巨大payload（文字列4000バイト超）・過大な配列長（64要素超）・配列内のNULL要素を拒否する ②その後に既存の`private.assert_offer_args()`が意味的な上限（文字数・要素数・数値範囲）を検証する。①は`btrim`・重複除去・fingerprint・マッチング計算より前に置く | 高コストなSQL処理（unnest+group byの重複除去、母集団全走査のマッチング）を、明らかに不正な入力で走らせない。NULL要素は重複除去でひとつに畳まれて後段の検証をすり抜けるため、前段で個別に拒否する。境界値・敵対的入力の検証は`17_min_audience_test.sql` |
| D040 | Accepted | メール通知の受け取り方を`each`（オファーごと）/ `daily`（1日1回のまとめ・Asia/Tokyo 18時）/ `off`（通知しない）の3種類とし、**行が無い学生の既定は`each`**とする。保存先は`public.student_notification_settings`で、興味パスポートとは別テーブルにする。学生本人だけがRLSで自分の行を読め、書込は`save_notification_settings` RPCのみ | 週間受信上限（1〜5件）があるため`each`でも過剰にならず、「オファー到着を知れる」という目的を既定で満たす。別テーブルにするのは、パスポートを削除しても通知設定を本人の管理下に残し、Task 014で削除の順序を独立に決められるようにするため。既定値はサーバー（enqueueの`coalesce`）とクライアント（`DEFAULT_NOTIFICATION_MODE`）の両方に同じ値を持ち、片方だけ変えると通知が復活する事故を防ぐ。正本は`docs/notifications.md` |
| D041 | Accepted | 送信は`private.email_outbox`を介する。outboxは**宛先メールアドレスも本文も保存しない**（宛先は送信時に`auth.users`から引き、本文は`kind`と件数から組み立てる）。`unique (kind, user_id, dedupe_key)`で二重に積まれない（`dedupe_key`は`offer_arrival`が配信ID、`daily_digest`がAsia/Tokyoの暦日）。積む契機は`private.offer_recipients`への挿入トリガ（文レベル・遷移テーブル）とし、`send_offer`は書き換えない。失敗は指数バックオフ（1/5/25/125/625分）で再試行し、5回を超えたら`failed`で止める。失敗理由は40文字までの分類コードのみ保存する | 同一トランザクションで積むため、送信が後段で失敗すればoutboxもrollbackされる。メール送信そのものはトランザクション外で行うため、**SMTP障害が配信トランザクションを壊さない**。`send_offer`を書き換えないのは、Task 011で確定した送信の判定・原子性・並行制御（D036・D037）に手を入れないため。SMTPの生メッセージは宛先・認証情報・本文断片を含み得るので保存しない（D029） |
| D042 | Accepted | 送信ワーカーはSupabase Edge Function（`app/supabase/functions/send-notifications/`）とし、既存のSMTP構成を使う。新しい有料サービスは導入しない。`claim_email_batch` / `complete_email` / `email_outbox_health`は**service_role専用**で、`authenticated`・`anon`へは一切grantしない。`claim_email_batch`が宛先メールを返す唯一の経路で、`FOR UPDATE SKIP LOCKED`によりワーカーが並んでも同じ行を二重に掴まない。メール本文には「届いた事実」「まとめの件数」「受信箱へのリンク」「通知設定へのリンク」だけを載せ、希望条件・団体名・イベント名・返答状態・マッチ度を載せない。配信停止はワンクリック解除ではなく通知設定画面への導線とする | メールボックスが共有・漏えいしても、どの団体から誘われどう返答したかが分からないようにする（matching_and_safety.md §7）。解除用トークンをメールへ埋めると、漏えい・誤操作・なりすましで第三者が通知を止められる経路になるため、本人のログインを経由させる。SMTP資格情報はSupabaseのsecret管理だけに置き、リポジトリ・CI・PR・ログへ出さない。設定不足は`missing_env:<名前>`として名前だけを出す。純粋関数（`emailTemplate.ts`）はVitestで検証済みだが、**`index.ts`の実送信はDockerとDenoが無いため未検証**でhosted stagingの人間タスク |
| D043 | Accepted | 団体状態（`pending` / `verified` / `suspended`）の変更・オファーの個別停止・全体の緊急停止は、**service_role専用RPCの4本**（`admin_set_organization_status` / `admin_set_offer_stopped` / `admin_set_delivery_paused` / `admin_list_audit`）だけが行える。`anon`・`authenticated`へは一切grantせず、`public.organizations`にも書込grantを与えない（読取のみ）。運営操作は`private.admin_audit_log`へ「操作・対象団体/配信・操作者ラベル・理由・変更前後の値・時刻」だけを記録し、**学生のuser_id・メール・氏名・希望条件・受信者一覧を持たない**（D029） | 団体が自分をverifiedにできる経路、他団体を止められる経路をどちらも作らない。admin権限をクライアント側の判定（画面の出し分け）だけに依存させない。操作者ラベルは氏名・メールを入れない運用とし、テストで`@`を含まないことを検査する。監査記録の列集合はpgTAPの完全一致リストで固定し、将来PII列が足されたら失敗する。運営画面UIはTask 013のスコープ外（安全なadmin経路とSQL手順まで）。正本は`docs/operations.md`、検証は`27_org_status_test.sql`・`29_admin_boundary_test.sql`。**`verified`以外へ変える操作（`suspended`だけでなく`pending`へ戻す場合も）は、その団体の配信中オファーをすべて止める**。独立レビューで、`pending`へ戻しても案内が生き続け、学生が返答でき「行ってみたい」で公式窓口まで開示され続けることを実証したため、「確認済みでない団体の案内は届いたままにしない」を一貫させた。**同じ不変条件を`admin_set_offer_stopped`側でも守り、確認済みでない団体の案内は個別にも戻せない**（`org_not_verified`）。戻す順序は「団体を`verified`へ戻す→個別の案内を戻す」。運営RPCは`actor_label` 60文字・`reason` 200文字を入口で検査して`invalid_admin_action`にする（生の制約違反にすると、DETAILへ対象行の全列が展開されてログに残る） |
| D044 | Accepted | 個別オファーの停止は`private.offer_deliveries.stopped_at` / `stopped_reason`で表し、停止しても**受信箱から消さない**。学生には「募集終了」として残し、①新しい返答を`offer_stopped`で拒否する ②既に「行ってみたい」を選んでいても公式窓口を返さない（`list_my_inbox`が空文字を返す）③既読の記録は妨げない。団体側の一覧にも`stopped`を返し「停止中」と表示する（停止理由の本文は返さない）。停止時点で送信待ちの`offer_arrival`メールは`cancelled`にする。**さらに`claim_email_batch`が送信直前にも停止済み配信の`offer_arrival`を取り消す**（停止時点で`sending`だった行や、一時失敗で`pending`へ戻った行が送られてしまうため。通知設定と同じ「送信時が権威ある関門」パターン） | 受信箱から消すと、学生には「見たはずの案内が消えた」としか分からず、何が起きたか説明できない。窓口を閉じるのは、停止の理由が団体側の問題であるときに接触を続けさせないため。停止理由は運営の内部記述であり団体へ開示する前提の欄ではないので返さない。まとめメール（`daily_digest`）は複数の案内をまとめた1行でどの案内を含むか保持しない設計のため取り消さないが、本文に団体名・イベント名を含まず受信箱側で「募集終了」と分かるため誤解を生まない。検証は`27_org_status_test.sql` |
| D045 | Accepted | 全団体の配信を1操作で止める緊急停止（kill switch）を`private.platform_controls`（単一行）に置き、判定は**`private.offer_deliveries`へのBEFORE INSERTトリガ**（`private.assert_delivery_allowed()`）で行う。トリガは①緊急停止中なら`delivery_paused` ②団体が`verified`でなければ`org_not_verified`を送出する。あわせて**`private.offer_preview_cache`へのBEFORE INSERTトリガ**（`private.assert_preview_allowed()`）で、緊急停止中は**新しい条件の対象規模も答えない**。24時間以内に答えた同一条件（キャッシュ命中＝挿入が起きない）は返す。両トリガとも`ENABLE ALWAYS`。安全装置の行が読めないとき（単一行の消失）は**止める側へ倒す**（fail closed）。`admin_set_delivery_paused`は0行更新なら`platform_controls_missing`を送出し、事実と食い違う監査記録を残さない | RPCの中だけに判定を書くと、将来別の挿入経路（管理スクリプト・新RPC・データ移行）ができたときに素通りする。構造で止めるため、`send_offer`を通さない直接insertでも止まることをpgTAPで検査する（テーブル所有者権限で試行）。停止中の送信はトランザクション全体がrollbackされるため、部分的な配信も枠の消費も残らない。`send_offer`側の既存verified判定は残し、二重に守る（防御の多層化）。previewまで止めるのは、独立レビューで「不正利用がpreviewでの探索そのものである場合、配信だけを止めても探索は続けられる」ことを実証したため。既に答えた条件を止めないのは、団体が既に知っている値であり新しい情報を渡さない一方、止めると緊急停止のたびに正常な団体の確認画面まで壊れるため。`ENABLE ALWAYS`にするのは、既定の`ENABLE`だと`session_replication_role='replica'`で不発になり得るため（当該GUCはsuperuser専用でクライアントから到達できないが、将来のレプリカ・ダンプ復元経路に備える）。検証は`28_kill_switch_test.sql`。`ENABLE ALWAYS`は`pg_restore --data-only --disable-triggers`と論理レプリケーションのapplyでも発火するため、data-only restoreの手順を`docs/runbook_supabase_hosted.md` §8へ記載する |
| D046 | Accepted | 本人による削除を3つに分ける。①`delete_student_passport`（興味パスポートのみ。以後は新規配信の対象にならないが、**受信済みの案内と返答は履歴として残る**・D023）②`delete_my_account`（CUEに保存した自分のデータをすべて削除）③`leave_organization`（所属を外す）。いずれも`authenticated`向けのSECURITY DEFINER RPCで、本人の行だけを対象とする。削除の記録は`private.deletion_audit_log`へ残し、**主体（誰が）を一切持たず、`(action, occurred_on)`の日次集計**（件数と削除行数の合計）だけを持つ。パスポート削除では、送信待ちの案内メール（`offer_arrival`の`pending`）も`cancelled`にする（「新しい案内は届かなくなります」の通り） | 当初は`user_id`のSHA-256を残したが、独立レビューで`join auth.users u on subject_hash = digest('cue-deletion-subject:'||u.id, 'sha256')`によりメールアドレスへ戻せることを実証された。saltもHMAC鍵も無く、プレフィックスは公開リポジトリの定数で、候補IDは**同じDBの中**にあり、D047の設計上退会後もauth.usersが残るため「退会直後の利用者」がもっとも確実に特定できてしまう。**同一DBにauth.usersがある限り、user_idの決定的な関数はどれも照合可能になる**ため、主体を持たない設計にした。日次集計にすることで、利用者1人が保存と削除を繰り返しても行が「操作の種類×日数」を超えて増えない（DoSの入口も塞ぐ）。運営が知りたい「削除が動いているか・どれだけ消えたか」には足りる。パスポート削除で受信済みを消さないのはD023の履歴固定と整合させるため。画面では消す前に「消えるもの／残るもの」を必ず出す（`features/account/deletion.ts`に文言を集約）。検証は`30_passport_delete_test.sql`・`32_membership_leave_test.sql` |
| D047 | Accepted | `delete_my_account`は`public.student_accounts`と`public.organization_memberships`の行を消す。学生側のデータ（パスポート・通知設定・受信枠・送信待ちメール・受信者行・既読・返答）はFKの`on delete cascade`で落ちるため、**孤児データが構造的に発生しない**。検証は特定のテーブルを列挙せず、**`user_id`列を持つ`public`・`private`の全テーブルを動的に走査**して残存ゼロを確認する。`auth.users`のidentity（大学メール）はRPCから消せないため残り、運営が`admin_delete_auth_identity`（service_role専用・CUEのデータが残っている間は`account_data_remains`で拒否）で仕上げる | 走査型にするのは、将来テーブルが増えたときに検査が自動的に対象へ含めるため（列挙型だと足し忘れに気づけない）。空振りで合格しないよう、走査対象が8テーブル以上あることと、削除していない利用者では走査が行を見つけることも同時に検査する。auth identityが残る点は**受入条件の検査範囲（`public`・`private`スキーマ）の外**だが、本人の大学メールが残るため運営手順を`docs/operations.md` §9へ置き、残余リスクとして記録する。**本番Supabaseでの`auth.users`削除の挙動（`postgres`のDELETE権限・`auth.identities`/`sessions`/`refresh_tokens`の連鎖・`auth.audit_log_entries`へのメール残存）は未検証で、hosted stagingで確認してから運用を開始する**（残る場合はAdmin APIへ寄せる） |
| D048 | Accepted | 団体からの脱退は、最後のownerだけができない。判定は既存の`private.protect_last_owner`トリガ（`organization_memberships`のBEFORE UPDATE OR DELETE）が持ち、RPC側に同じ判定を重ねて書かない。`delete_my_account`も、最後のownerである団体があるとトリガが`last_owner`を送出してトランザクション全体が巻き戻る（部分削除は起きない）。**団体そのものの削除はTask 014のスコープ外**とする | RPCへ事前チェックを書いても、トリガと同じエラーしか出せず観測上の差が無い（mutation testで実証: 事前チェックを消してもテストは全件通った）。テストが検知できない重複コードを残さず、構造の保証をトリガ側へ一本化する。トリガ自体はRPCを通さない直接delete・直接updateでも止まることをpgTAPで検査する。団体削除を外すのは、最終ownerガードと配信snapshot（D023）の整合を別途設計する必要があるため |
| D049 | Accepted | `delete_my_account`は、本人が**唯一の代表者になっている団体の所属だけを残し**、それ以外（学生側の全データと、代表者が他にいる団体の所属）を消す。戻り値で「消した行数」と「外せなかった団体の数」を返す。最終ownerガード（`private.protect_last_owner`トリガ）は並行実行でも破れないよう、存在判定に`for update`を足して直列化し、トリガ自体を`ENABLE ALWAYS`にする | 当初は所属を全部消そうとし、最後のownerの団体が1つでもあるとトランザクション全体が巻き戻って**学生側のデータも1件も消えなかった**（独立レビューH-1で実証）。団体を1人で作った担当者は`create_organization`の仕様上ほぼ全員が単独ownerで、ownerを増やす経路が無い（招待はownerを招待できず、role変更RPCも無い）ため、「退会も、学生としてのデータ削除も、運営経由の削除も一切できない」利用者が構造的に生まれていた。新入生としてのデータと団体担当者としての所属は別のものなので、前者の削除を後者の事情で止めない。並行脱退はロックを取らない存在判定だと2人が同時に抜けてowner 0人になり（独立レビューB1で実証。復旧経路が無い）、`for update`で直列化する。`ENABLE ALWAYS`は、このトリガが最終ownerガードの唯一の防御になったため（Task 014でRPC側の重複判定を削除した）、Task 013の2トリガと基準を揃える。検証は`32_membership_leave_test.sql`と`scripts/concurrency_test.sh`の並行脱退ケース。**残る課題**: 単独ownerの団体を閉じる・代表者を交代する経路は未実装で、当面は運営がSQLで対応する（`docs/operations.md` §9） |
| D050 | Accepted | 利用規約とプライバシーポリシーを**ドラフト**として`docs/legal/`に置き（法令適合は断定せず、`【要確認】`で法的判断が必要な箇所を明示する）、同意を1つの単調増加の整数版で管理する。`private.current_consent_version()`が現在必要な版を返し、`public.student_consents`（user_id・版・時刻のみ）へ本人の最新同意版を記録する。**同意していないと、「参加を作る・広げる」操作は一切できない**（`save_student_passport`・`create_organization`・`accept_invitation`・`create_invitation`・`update_organization_profile`・`update_organization_contact`・`preview_offer_audience`・`send_offer`の8つのRPC内で`consent_required`を送出。画面の出し分けだけに頼らず構造で止める）。逆に**利用者が自分の露出を減らす操作は、版が上がっても止めない**（`respond_to_offer`・`mark_offer_read`・`save_notification_settings`・`delete_student_passport`・`leave_organization`・`delete_my_account`・`revoke_invitation`）。ゲート対象の一覧はカタログ走査で固定する（増えても減ってもテストが落ちる）。版を上げると、同意済みの利用者も`my_consent().needs_consent`がtrueになり再同意を求められる。`record_consent`は画面が提示した版と現在の版が食い違うと`consent_version_mismatch`で拒否する。アカウント削除で同意記録も消す（`student_consents`は`auth.users`を参照するため`student_accounts`のcascadeでは落ちない） | 「登録前に何が誰に見えるか・いつ消せるか」を理解してから登録させる。利用規約は新入生と団体担当者の両方に及ぶため、担当者になる経路（作成・招待承諾）と団体側の書込・プレビューをすべて同意必須にする。**独立レビューで、招待経由の未同意担当者が団体名・公式窓口の変更、招待の発行まで到達できることを再現した**（画面はPostgRESTの直接呼び出しを防げない）ため、当初の2 RPCから8 RPCへ広げた。断る・止める・消す操作を同意の人質にすると「いつでも受信を止められる」という約束を壊すため、露出を減らす方向の操作はゲートしない。なお受信の**一時停止**は`save_student_passport`経由のため版更新後は再同意が必要になるが、`delete_student_passport`（完全停止）は常に開いているため、止める手段は失われない。版を整数1本にするのは、規約とポリシーを画面で同時に提示し、まとめて再同意させるため。同意記録に希望条件・メールを持たせない（D029）。**法令適合は運営者の最終確認事項**（`docs/launch_plan.md` H4）で、事業者情報・連絡先・準拠法・委託先はプレースホルダーとして明示する。文書の記述と実装の一致は`tasks/015-privacy-and-consent.md`の対応表で確認する。検証は`33_consent_test.sql`（同意前の書込拒否・版更新での再同意・RLS・PIIサーフェス）と`consentContent.test.ts` |
| D051 | Accepted | E2Eの失敗アーティファクトへ秘密値を残さないため、`trace`/`video`/`screenshot` の無効化に加えて `PLAYWRIGHT_NO_COPY_PROMPT` を立てる（`app/playwright.config.ts` の冒頭で `??=` により既定値を設定し、CIの e2e ジョブでも `env:` で指定して二重化する）。あわせて `app/e2e/` を `tsc -b` のproject referenceへ入れ、E2Eの型検査を恒常化する | **Playwrightは、テストが失敗すると既定で `page.ariaSnapshot()` を撮って `test-results/<test>/error-context.md` へ書き出す**。これは `trace`/`video`/`screenshot` の設定とは無関係で、この経路のgateは `PLAYWRIGHT_NO_COPY_PROMPT` だけである（本リポジトリが固定している1.62.1で確認）。**別経路として `toMatchAriaSnapshot` の失敗があり、そちらは環境変数で止まらない**（失敗時のaria yamlが `# Error details` へ埋め込まれ、CIログにも出る）。OTP・招待URLが表示されている画面でこのマッチャを使わない。aria snapshotは `input`/`textarea` の**値をそのまま含む**ため、6桁コードの入力直後や招待URLの表示中に失敗すると、それらがファイルへ落ちる。**実際に走らせて確認した**: 既定では `- textbox "code" [active]: "123456"` が残り、環境変数を立てるとファイルは作られるがページ内容は一切入らない（`grep -c` で0）。`test-results/` はgitignore済みでCIに `upload-artifact` も無いが、「秘密値が残らない」と書いてある以上、実態を合わせる。E2Eの型検査は `npm run build`（`tsc -b`）が `e2e/` を見ておらず、壊れた型がCIをすり抜けていた（一時tsconfigでの手元検査に依存していた）。参照追加後、故意の型エラーで実際に落ちることを確認した |
| D052 | Accepted | 運用の可観測性を、**service_role専用の1本のRPC** `public.platform_health()` に集約する（緊急停止の状態・審査待ち団体数・outboxのpending/failed/滞留・最古pendingの**予定時刻からの超過分**・quota超過数・期限切れpreview数・監査行数・**認証側の兆候4件**。時刻ではなく件数で返す）。返すのは**件数と時刻だけ**で、個人情報を含めない。**認証の「生存」はDBのRPCでは確認できない**（Auth APIを叩けない）ため、返すのはDBから観測できる兆候にとどめ、生存確認はOTPを往復させる人間の手順（`docs/runbook_incident.md` §2.5）に置く。`quota_over_limit` は、**本人が後から受信上限を下げただけ**の正常操作を数えない（最後の配信より後にパスポートが更新された行を除外する）。監査ログの保持期間を**365日**と決め、`private.prune_audit_logs(retain_days)` で剪定する。剪定関数と `private.prune_preview_cache(retain_hours)` は **anon・authenticated・service_role のいずれからも実行できない**（DB管理者＝DashboardのSQL Editorのみ）。保持期間の下限（監査365日→30日未満は拒否・preview 24時間未満は拒否）を関数内で強制する。migrationの連番は0016を飛ばして0017にする | Task 013・014で「監査記録の保持期間・削除方針が未定」と残した残余リスクを閉じる。健全性の確認を1本にまとめるのは、運営が毎日見る手順を短くするため（`docs/runbook_operations.md` §8）。**外部の有料監視サービスを使わない**方針（Task 017のGoal）の下では、「1本のSQLを毎日見る」が現実的な最小構成になる。剪定をservice_roleからも呼べなくするのは、**監査は消しにくいほうがよい**ため。誤操作で監査が消えると、運営操作の追跡ができなくなる。`quota_over_limit` は設計上0のはずで、1以上は枠確保（`FOR UPDATE`）が壊れている合図として使う。連番を0016でなく0017にするのは、Task 016がmigrationを持たず、連番とタスク番号を一致させたほうが追跡しやすいため。検証は `34_ops_health_test.sql` 29件（権限固定・**PUBLIC不在**・返す列の固定・監査ログの列の固定・保持期間の境界・**各列の値の実測**）。独立レビューが「7列をリテラル0で返す変異体が全件pass」「PUBLICへgrantしても全件pass」を実証したため、どちらも落ちるところまで強化した |
| D053 | Accepted | 送信し終えた通知メールの行を `private.prune_email_outbox(retain_days)`（既定90日・下限7日）で剪定する。消すのは `sent` / `failed` / `cancelled` で、かつ `updated_at` が保持期間より古い行だけ。**`pending` と `sending` はどれだけ古くても消さない**。実行権限は `prune_audit_logs` と同じく **anon・authenticated・service_role のいずれからも与えず**、DB管理者（DashboardのSQL Editor）のみとする。あわせて送信ワーカーの並行取り出し（`claim_email_batch` の `for update skip locked`）を `scripts/concurrency_test.sh` の第4フェーズで実際に2プロセス並行させて検証し、`src/lib/database.types.ts` を `supabase gen types` の出力へ同期する | Task 010が「古いoutbox行を消す経路が無い」と残し、Task 017が既知リスクE5として引き継いだ項目を閉じる。`email_outbox` は `user_id` を持つため、無期限に増やさない。**終わっていない行を消すと、その学生には通知が二度と届かない**（`sending` はワーカーが落ちた行を `claim_email_batch` が拾い直す対象でもある）ため、状態での絞り込みを消す条件の中心に置く。二重送信にならない根拠は、outboxへ積む経路が `private.offer_recipients` のAFTER INSERTトリガ1本だけで、受信者行が配信時に1度しか入らないこと（`offer_arrival` の `dedupe_key` は配信ID、`daily_digest` は送信日）。剪定をservice_roleからも呼べなくするのはD052と同じ理由。ワーカーの並行検証をpgTAPではなくシェルへ置くのは、pgTAPが1ファイル=1セッションで**本物の競合を作れない**ため（`skip locked` を外した変異体が「Bがロックを待たない」1件で落ちることを確認した）。生成型の同期は、CIの差分検査が警告を出し続けると本当の差分に気づけなくなるため。検証は `35_outbox_prune_test.sql` 14件（権限・PUBLIC不在・下限の境界・状態ごとの取捨・**pending/sendingが400日前でも残ること**・積む経路が増えていないこと・宛先/本文の列が無いこと） |
| D054 | Accepted | 公開ビルドへ渡す `VITE_SUPABASE_URL` と `VITE_SUPABASE_PUBLISHABLE_KEY` を、GitHub Actions の **variable ではなく secret** へ置く（`docs/runbook_operations.md` §2 の「ブラウザへ出る値なのでsecretではなくvariableでよい」を上書きする。D027自体は`VITE_*`がバンドルへ入る事実の決定で、置き場所は定めていない）。あわせて公開前の検証ステップで、URLを正規表現 `^https://[a-z0-9-]+\.supabase\.co/?$` で、鍵を許可リスト `sb_publishable_*` で検査し、検査に落ちたときは**その鍵をSupabase Dashboardで失効（rotate）してから設定し直す**ことをメッセージで指示する。`workflow_dispatch` に `dry_run` 入力を足し、公開せずにbuildと検証だけを実行できるようにする | 判断の軸は値の秘匿ではなく**貼り間違いの封じ込め**。**runnerは各stepの冒頭に `env:` マップを値ごと出力し、`secrets.*` はマスクされるが `vars.*` はマスクされない。**本リポジトリは公開なのでActionsログは誰でも読める。SupabaseのAPI keys画面は `sb_publishable_...` と `sb_secret_...` を並べて表示し、どちらも `sb_` で始まる。variableのままだと、`sb_secret_*` を貼った時点で、検証ステップがdeployを止める**前に**Build stepが平文で世界へ出してしまう（独立レビューが実証）。secret keyはRLSを迂回するため、出た時点で全学生のデータが誰からでも読み書き可能になり、revertでは戻せない。secretへ置けばマスクされ、貼り間違いが致命傷にならない。**公開値をsecretへ置くことで失うものは無い**（バンドルへ入る事実は変わらない）。URLの検査を `case` のglobから正規表現へ変えたのは、globの `*` が `/` `?` `#` を跨ぎ、`https://evil.example.com/.supabase.co` が通ってしまうため（同レビューが実証。`supabaseClient.ts` はURLを検証せず `createClient` へ渡すので、抜けると全通信の宛先が差し替わる）。`dry_run` を足したのは、`workflow_dispatch` が**選んだrefのワークフロー定義**を実行するため、`main` を選ぶと検証ステップを持たない旧定義が走り「通った」という誤った確信だけが残るため（同レビューが実証）。鍵の許可リストは**貼り間違い対策であって、悪意ある値の検査ではない**（`sb_publishable_` を前置しただけの文字列は通る）。検証は `.github/workflows/deploy-pages.yml` と `tasks/018-release-v1.md` の実測表 |
| D055 | Accepted | `public.claim_email_batch(batch_size)` が掴む行を確定する処理を、`where o.id in (select ... for update skip locked limit batch_size)` から **`with picked as materialized (...)` のCTE + join** へ変える。並び順も `order by c.next_attempt_at, c.id` として同着を決定的にする。検証は `36_claim_batch_limit_test.sql` 12件（batch_sizeちょうど / 続けて呼ぶ / 二重に掴まない / 境界1・200 / 0・201の拒否 / **実装が `as materialized` であることのカタログ固定** / **同着順 `order by c.next_attempt_at, c.id` のカタログ固定**） | **CIで batch_size=3 に対して6件掴む事象を観測した**（`count=6` / `sending=6` / `attempts=1` / もう一方のワーカー=0）。`FOR UPDATE SKIP LOCKED` + `LIMIT` を IN サブクエリへ置くと、プランによってはサブプランが**再実行**される。テストデータは6行を同一文で挿入するため `next_attempt_at` が全て同値で、**同着の並び順が不安定**。再実行のたびに別の3件が返れば UPDATE は最大6件に一致し、観測値のすべてと整合する。二重送信は起きていない（`attempts=1`）が、「1回の取り出しは batch_size 件まで」という契約が破れており、ワーカーが想定より多くの宛先を1回で抱える（Gmailの日次上限・§7.1 E6 と複合する）。CTEの実体化はキュー実装の定石で、再実行が起きない。**ローカルのPostgreSQL 16では再現せず、CIのスタックでのみ出た＝プラン依存**のため、振る舞いのテスト（1〜10）は旧実装でもローカルでは通る。回帰を確実に捕まえるのは**実装構造を固定するテスト11**で、旧実装へ戻すと実際に落ちることを確認した。migrationの連番は0019。**D054はTask 018（PR #22・レビュー中）が使用しており、mergeの順序（#24が先）により本決定の時点では一時的に欠番になる**（D052で0016を欠番と明記した慣行に合わせて記録する） |
