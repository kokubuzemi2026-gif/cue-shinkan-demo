# AGENTS.md

このファイルはリポジトリ全体に適用します。

## 1. 正本と読む順番

作業開始時は、次の順番で必要な文書を確認してください。

1. `README.md`
2. `docs/decisions.md`
3. `docs/agent_harness.md`（自律実行の手順・モード・完了の定義）
4. `docs/product_spec.md`
5. `docs/matching_and_safety.md`
6. `docs/auth_and_authorization.md`（Phase 2の認証・権限の正本）
7. 対象となる `tasks/NNN-*.md`

古い会話やAIの記憶より、GitHub上の最新仕様を優先します。記憶にあるSHA・ブランチ名・実装状況を信用せず、`develop`の最新・open PR・CIの状態を確認してから着手してください。

文書間に矛盾がある場合、受入条件を左右する矛盾であれば作業を止めて報告します。表記ゆれなど軽微な差異は、最小の解釈で進めたうえで、その解釈を報告に残してください。

## 2. タスクの境界

- 指定されたタスクだけを実行する
- 指定されていない画面、機能、依存関係を追加しない
- 対象外ファイルを変更しない
- 大規模なリファクタリングを同時に行わない
- 未決事項を「一般的には」で補完しない
- 1タスク・1ブランチ・1PRを原則とする
- ハーネス（`CLAUDE.md` / `AGENTS.md` / `docs/agent_harness.md` / `.claude/`）の変更は、
  実装タスクのPRに混ぜず、独立したPRで行う

## 3. 自律実行と作業前の提示

編集前に、次を短く提示してください。

- 理解した目的
- 読んだ仕様書
- 変更予定ファイル
- 実装方針
- 未解決の疑問またはリスク

提示は記録のために行います。**承認を待って停止しないでください。**第4節に該当する疑問がある場合だけ、実装前に質問します。それ以外は最小の解釈で実装し、解釈を報告に残してください。

実行モード（Fast / Standard / Deep）の選び方と、Plan → Implement → Verify → Review → Repair のループは `docs/agent_harness.md` に定義しています。

## 4. 人間へのエスカレーション

次の場合**だけ**作業を止めて人間に確認します。

- OAuth / OTP / 2FA など、人間の操作や本人所有アカウントが必要な認証
- 課金、有料プラン、外部サービスの新規契約
- production環境への不可逆な変更（データ削除、公開設定変更、DNS、鍵の失効）
- 正本の文書間に、受入条件を左右する重大な矛盾がある
- タスクの前提が現状と食い違い、続行するとスコープを大きく越える
- 同じ失敗に対する修正が3回連続で失敗した
- リポジトリ・CI・公開物に、実在する個人情報またはsecretが含まれている疑いがある

次では停止しません。自分で調べて直します。

- コマンドの失敗、依存関係の不足、環境変数の未設定（実値そのものが必要な場合を除く）
- lint / test / build / CIの失敗
- レビュー指摘、設計のやり直し
- 想定より変更点が多いこと（タスク境界の中に収まる限り）

エスカレーションでは、質問だけを投げず「確認した内容」「決められない点と選択肢」「推奨」を添えてください。

## 5. 独立レビュー

commitとPRの前に、実装者とは独立した観点でレビューします。Claude Codeでは `.claude/agents/` のsubagentを使います。

- `reviewer`: 仕様適合・スコープ・品質・回帰リスク（Standard以上で必須）
- `security-reviewer`: 認証・権限・DB・RLS・RPC・migration・Edge Function・通知・PII・secret
  （これらに触れる変更では必須）

レビューを依頼するときは、自分の結論に同意を求めないでください。差分と正本を渡し、独立に検証させます。「テストが通った」という主張は証拠になりません。レビュー側が自分で実行して確認します。

Blockerが1件でも残っている状態で、完了と報告しないでください。

## 6. プライバシーと安全

- 実在する学生、団体メンバー、連絡先をテストデータに使わない
- 氏名、顔写真、学籍番号、電話番号、メールアドレスを初期マッチングに使わない
- 性別、国籍、人種、宗教、病歴などによるターゲティングを追加しない
- シークレットやAPIキーをコミットしない
- 団体側に個別学生の一覧を公開する変更は、明示的な仕様変更なしに行わない
- ブロック、通報、受信停止など利用者のコントロールを壊さない

## 7. UI/UX

- スマートフォンを第一画面とする
- 主要操作は片手で完結できる位置とサイズにする
- オファーへの返答は「承諾／拒否」の二択にせず、保留を用意する
- マッチ理由と、必要なら不一致条件も表示する
- 圧迫感、煽り、過度な通知、断りづらい表現を避ける
- 色だけに意味を依存させない

## 8. 品質確認

変更後は利用可能な範囲で次を実行してください。

- formatter
- lint
- typecheck
- unit test
- build

変更内容に応じて追加します。

- DB・RLS・RPC・migrationに触れた: pgTAP（`npm run db:test`）
- 認証・主要導線のUIに触れた: E2E（`npm run e2e`）
- `.claude/hooks/` に触れた: `python3 .claude/hooks/test_hooks.py`

UI変更では、少なくともスマートフォン幅で主要導線を確認してください。

テストを弱体化して通さないでください（`skip` / `only` / アサーション削除 / 期待値の緩和）。実行できなかった検証は「未検証」として残し、合格扱いにしないでください。

## 9. Definition of Done

次をすべて満たしたときだけ「完了」と報告します。

1. 受入条件を1つずつ確認し、充足・不足・未検証を明示した
2. スコープ外のファイルを変更していない
3. `app/` 変更時: lint / unit test / build がローカルでgreen
4. 変更内容に応じた追加検証を実行、または未実施として明示した
5. 独立レビューを通し、Blockerが残っていない
6. secret・実在する個人情報・APIキーをコミットしていない
7. feature branchへpushし、`develop` 向けPRを作成した
8. CIがgreen、または赤の原因と対応方針をPRへ記録した
9. 日本語で報告した

自動マージはしません。マージの判断は人間が行います。

## 10. 完了報告

次の形式で報告してください。

1. 変更概要
2. 変更ファイル
3. 検証結果（未実施のものは「未実施」と明記）
4. 仕様との対応
5. 独立レビューの結論と対応
6. PR / ブランチ / commit / CI
7. 残るリスク・未実施事項
