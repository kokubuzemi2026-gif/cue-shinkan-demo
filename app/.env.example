# Supabase接続設定（ブラウザへ公開してよい2値だけを置く）
# 実値は .env.local へ書く（gitへコミットしない）。
# ローカル開発では `npm run db:start`（supabase start）の出力から転記する。
# secret key / service-role key / DBパスワード / アクセストークンは絶対に置かない。
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
